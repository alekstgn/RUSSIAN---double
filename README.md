# 🧙‍♂️ Мудрецы AI - Telegram бот

Telegram-бот, который предоставляет доступ к двум российским AI-моделям: **GigaChat** и **YandexGPT**. Бот стилизован под древних мудрецов, которые отвечают на вопросы пользователей мудро и с достоинством.

## ✨ Функции

- 🔄 **Переключение между AI-моделями**: Выбор между GigaChat и YandexGPT
- 🧙‍♂️ **Стилизованные ответы**: Ответы в стиле древних мудрецов
- 📊 **Детальное логирование**: Отслеживание использования токенов и запросов
- 🎯 **Удобное управление**: Интуитивные кнопки для управления ботом

## 📋 Требования

- Python 3.7+
- Telegram Bot Token
- Yandex Cloud API Key и Folder ID
- GigaChat Credentials

## 🛠 Установка

1. **Клонирование репозитория**:
```bash
git clone <url-репозитория>
cd "RUSSIAN — double"
```

2. **Создание виртуального окружения**:
```bash
python -m venv venv
```

3. **Активация виртуального окружения**:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

4. **Установка зависимостей**:
```bash
pip install -r requirements.txt
```

5. **Настройка переменных окружения**:
Скопируйте файл `env.example` в `.env` и заполните реальными значениями:
```bash
cp env.example .env
```
Затем отредактируйте файл `.env`:
```env
TELEGRAM_TOKEN=your_telegram_bot_token
YANDEX_FOLDER_ID=your_yandex_folder_id
YANDEX_API_KEY=your_yandex_api_key
GIGACHAT_CREDENTIALS=your_gigachat_credentials
GIGACHAT_MODEL=GigaChat:latest
```

6. **Добавление сертификата**:
Поместите файл `russian_trusted_root_ca.cer` в корень проекта для работы с GigaChat.

## 🚀 Запуск

### Основной бот
```bash
python giga_bot.py
```

### Вспомогательные утилиты

**Тестирование YandexGPT**:
```bash
python generate-deferred.py
```

**Получение токена GigaChat**:
```bash
python get_token.py
```

## 📱 Использование бота

1. **Запуск**: Отправьте команду `/start` боту
2. **Выбор мудреца**: Используйте кнопки для выбора между GigaChat и YandexGPT
3. **Задавайте вопросы**: Просто отправьте сообщение боту
4. **Управление**:
   - 🔄 **Сменить мудреца** — переключение между AI-моделями
   - ℹ️ **Помощь** — показать справку
   - 🎯 **Текущий мудрец** — узнать активную модель

## 📁 Структура проекта

```
RUSSIAN — double/
├── giga_bot.py              # Основной файл Telegram бота
├── get_token.py             # Утилита для получения токена GigaChat
├── generate-deferred.py     # Утилита для тестирования YandexGPT
├── requirements.txt         # Зависимости проекта
├── env.example             # Пример файла конфигурации
├── bot_log.txt             # Лог файл бота
├── venv/                   # Виртуальная среда
└── README.md               # Документация проекта
```

## 🔧 Зависимости

- `gigachat==0.1.40` - Библиотека для работы с GigaChat API
- `python-dotenv==1.0.1` - Загрузка переменных окружения
- `pyTelegramBotAPI==4.15.2` - Telegram Bot API
- `yandex-cloud-ml-sdk==1.0.0` - Yandex Cloud ML SDK

## 📋 Логирование

Бот ведет детальное логирование:
- Информация о запросах к AI-моделям
- Использование токенов
- Ошибки и исключения
- Выбор пользователями моделей

Логи сохраняются в файл `bot_log.txt` и выводятся в консоль.

## 🔒 Безопасность

- Все конфиденциальные данные хранятся в файле `.env`
- Используется SSL-сертификат для безопасного соединения с GigaChat
- Логирование не включает персональные данные пользователей

## 🛠 Разработка

Для разработки рекомендуется:
1. Использовать виртуальное окружение
2. Следить за логами для отладки
3. Тестировать обе AI-модели перед внесением изменений

## 📝 Примечания

- Проект использует только российские AI-модели
- Стилизация ответов настроена для создания атмосферы древней мудрости
- Бот поддерживает обработку сигналов завершения для корректного выключения

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте правильность настройки переменных окружения
2. Убедитесь в наличии всех необходимых сертификатов
3. Проверьте логи для диагностики ошибок 